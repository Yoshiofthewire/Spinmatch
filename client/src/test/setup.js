// Runs before every test file (see test.setupFiles in vite.config.js).
// jest-dom adds matchers like toBeInTheDocument()/toHaveClass() to vitest's
// `expect` — without this import every assertion below would have to fall
// back to raw DOM poking (element.className.includes(...)).
import '@testing-library/jest-dom/vitest';
