# Node modules
node_modules/
**/node_modules/

# Environment files
.env
.env.local
.env.*.local

# Build artifacts
client/dist/
dist/
build/
*.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Runtime generated database files
# Runtime SQLite index / admin-credential DB when LIBRARY_DB is left at its
# cwd-relative default (matches both repo-root and server/ working dirs).
data/

# OS / editor files
.DS_Store
.vscode/
.idea/
coverage/

# Scratch output from browser-automation tooling (snapshots, screenshots, logs)
.playwright-mcp/

# Superpowers session scratch. The spec and plan under docs/superpowers/ are
# the deliberate, tracked artifacts; everything here is working notes.
.superpowers/
